<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.aikon.wht.dao.extend.ArticleLikeExtendMapper" >

    <insert id="upsert">
    insert into kyoumi_article_like (status,individualId,articleId)
    values (#{status},#{individualId},#{articleId})
    ON DUPLICATE KEY UPDATE
    status = #{status},
    individualId = #{individualId},
    articleId = #{articleId}
    </insert>
</mapper>